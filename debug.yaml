---
- name: Debug AAP
  hosts: all
  tasks:
    - debug:
        var: vars
        
    - set_fact:
        job_id: "{{ lookup('env', 'JOB_ID') | int }}"
        CONTROLLER_HOST: '192.168.1.9'
        CONTROLLER_USERNAME: admin
        CONTROLLER_PASSWORD: primod123
    - name: Load the UI settings specifying the connection info
      set_fact:
        controller_settings: "{{ lookup('ansible.controller.controller_api', 'settings/ui', username=admin, password=primod123, verify_ssl=False) }}"    

    - set_fact:
        job_api: "{{ lookup('ansible.controller.controller_api', 'jobs/{{ job_id }}', verify_ssl=False) }}"

    - debug:
        var: job_api.summary_fields.inventory.name
